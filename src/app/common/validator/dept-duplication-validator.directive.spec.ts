/* tslint:disable:no-unused-variable */

import { TestBed, waitForAsync } from '@angular/core/testing';
import { DeptDuplicationValidatorDirective } from './dept-duplication-validator.directive';

describe('Directive: DeptDuplicationValidator', () => {
  it('should create an instance', () => {
    const directive = new DeptDuplicationValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
